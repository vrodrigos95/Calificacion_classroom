# Copia este archivo a backend/.env y llena los valores. Nunca lo subas a git.

# --- Google OAuth (Google Cloud Console → APIs y servicios → Credenciales) ---
GOOGLE_CLIENT_ID=43275613375-7sspgkddggipl98i8quuenv028dcj6sp.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=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
OAUTH_REDIRECT_URI=http://localhost:5173/api/auth/callback
FRONTEND_URL=http://localhost:5173

# --- Sesión y cifrado ---
# python -c "import secrets; print(secrets.token_urlsafe(48))"
SESSION_SECRET=
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
TOKEN_ENCRYPTION_KEY=
SESSION_HTTPS_ONLY=false

# --- Base de datos y archivos ---
DATABASE_URL=sqlite:///./data/app.db
DATA_DIR=./data

# --- Claude (desde la etapa 3) ---
ANTHROPIC_API_KEY=
CLAUDE_MODEL=claude-opus-5

# --- Defaults de docente (cada docente puede cambiarlos) ---
RETENTION_DAYS=14
MINOR_ERROR_FACTOR=0.5
MARK_ACCEPT_THRESHOLD=0.85
MARK_DOUBTFUL_THRESHOLD=0.60
EXERCISE_MIN_CONFIDENCE=0.80

LOG_LEVEL=INFO
